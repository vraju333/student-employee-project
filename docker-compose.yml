services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: student_employee_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - student_employee_pg:/var/lib/postgresql/data
volumes:
  student_employee_pg:
